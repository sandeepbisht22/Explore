import Box from "@mui/material/Box";
import MapWithGeoJson from "./MapWithGeoJson";
type Props = {};

const RootMap = (props: Props) => {
  return (
    <Box>
      <MapWithGeoJson />
    </Box>
  );
};

export default RootMap;
