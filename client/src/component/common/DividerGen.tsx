import { Divider } from "@mui/material";
type Props = {};

const DividerGen = (props: Props) => {
  return (
    <Divider color="black" sx={{ lineHeight: "5rem" }}>
      X
    </Divider>
  );
};

export default DividerGen;
